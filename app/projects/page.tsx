 "use client"; import {useState} from "react"; import {projects} from "@/lib/data"; import {ProjectCard} from "@/components/ProjectCard";
export default function Projects(){const cats=["All","Residential","Kitchen","Commercial","Apartment","Bedroom","Villa"];const [cat,setCat]=useState("All");const shown=cat==="All"?projects:projects.filter(p=>p.category===cat);return <main className="pt-[76px]"><section className="container-nx py-24 md:py-36"><p className="text-xs font-bold uppercase tracking-[.25em] text-olive">Selected projects</p><h1 className="display mt-6 font-display text-7xl md:text-9xl">Spaces we've<br/><i>shaped.</i></h1></section><section className="container-nx pb-24"><div className="mb-12 flex flex-wrap gap-2">{cats.map(c=><button key={c} onClick={()=>setCat(c)} className={`rounded-full border px-5 py-2.5 text-xs font-bold uppercase tracking-widest ${cat===c?"bg-ink text-white":"border-black/15"}`}>{c}</button>)}</div><div className="grid gap-x-7 gap-y-14 md:grid-cols-3">{shown.map(p=><ProjectCard key={p.slug} project={p}/>)}</div></section></main>}
